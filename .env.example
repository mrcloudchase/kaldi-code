# Anthropic (default provider)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (for /provider openai)
OPENAI_API_KEY=sk-...

# Ollama (local, no key needed — just run `ollama serve`)
# KALDI_PROVIDER=ollama
# KALDI_MODEL=llama3
